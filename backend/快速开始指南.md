# 快速开始指南

## 前置要求

1. **Node.js 18+** 
   - 下载：https://nodejs.org/
   - 验证：`node --version`

2. **MongoDB**
   - macOS: `brew install mongodb-community`
   - 启动：`brew services start mongodb-community`
   - 或直接运行：`mongod`

3. **npm**（随Node.js安装）

## 安装步骤

### 1. 进入后端目录
```bash
cd backend
```

### 2. 安装依赖
```bash
npm install
```

### 3. 配置环境变量
```bash
# 复制环境变量模板
cp .env.example .env

# 编辑.env文件，修改以下配置：
# - MONGODB_URI: MongoDB连接地址
# - JWT_SECRET: JWT密钥（生产环境必须修改）
# - ENCRYPTION_KEY: 加密密钥（32字节，生产环境必须修改）
```

### 4. 创建上传目录
```bash
mkdir -p uploads
```

### 5. 启动服务器
```bash
# 开发模式（自动重启）
npm run dev

# 生产模式
npm start
```

服务器将在 `http://localhost:3000` 启动。

## 测试API

### 1. 健康检查
```bash
curl http://localhost:3000/health
```

### 2. 用户注册
```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "123456"
  }'
```

### 3. 用户登录
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "123456"
  }'
```

返回的token用于后续API调用。

### 4. 获取用户信息（需要token）
```bash
curl http://localhost:3000/api/auth/me \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

## 常见问题

### 1. MongoDB连接失败
- 检查MongoDB是否运行：`brew services list`
- 检查连接字符串：`MONGODB_URI=mongodb://localhost:27017/traffic_control`

### 2. 端口被占用
- 修改 `.env` 中的 `PORT=3000` 为其他端口

### 3. 文件上传失败
- 检查 `uploads/` 目录权限
- 检查文件大小限制：`MAX_FILE_SIZE`

### 4. JWT验证失败
- 确保请求头格式正确：`Authorization: Bearer <token>`
- 检查token是否过期

## 开发建议

1. **使用Postman或Thunder Client**测试API
2. **查看控制台日志**了解请求处理过程
3. **检查MongoDB数据**：`mongosh traffic_control`
4. **阅读代码注释**了解实现细节

## 下一步

1. 集成AI模型（误导检测、合同解析）
2. 添加单元测试
3. 部署到生产环境
4. 配置HTTPS和域名

## 获取帮助

- 查看 `README.md` 了解项目结构
- 查看 `API需求分析.md` 了解API接口
- 查看 `数据库设计.md` 了解数据模型
- 查看 `Node.js概念解释-C语言视角.md` 理解Node.js概念
